packages:
  - "."

# NOTE: this project pins pnpm@8 (see package.json packageManager), and pnpm 8
# does NOT read overrides from this file — security overrides live in
# package.json under "pnpm.overrides". A previous overrides block here
# (including a malformed "hono:" key) was inert and has been removed.
